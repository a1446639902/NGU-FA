<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yidu.businessData.mapper.CashClosePayMapper">

    <insert id="insertCash" parameterType="com.yidu.businessData.pojo.CashClosePayPojo">
        insert into cashClosedPay values (#{cashClosedPayId},#{fundId},#{accountId},#{securitiesId},#{serviceType},#{amount},#{dateTime},#{flag})
    </insert>

    <update id="updateCash" parameterType="com.yidu.businessData.pojo.CashClosePayPojo">
        update cashClosedPay set cashClosedPayId=#{cashClosedPayId},fundId=#{fundId},accountId=#{accountId},securitiesId=#{securitiesId},serviceType=#{serviceType},amount=#{amount},dateTime=#{dateTime},flag=#{flag}
    </update>

    <delete id="deleteCash" parameterType="INTEGER">
        delete from cashClosedPay where cashClosedPayId=#{cashClosedPayId}
    </delete>

    <select id="selectCash" parameterType="java.util.Map" statementType="CALLABLE">
        {call pkg_tables.sp_select(#{p_tableName,jdbcType=VARCHAR,mode=IN},#{p_condition,jdbcType=VARCHAR,mode=IN},#{p_pageSize,jdbcType=INTEGER,mode=IN},#{p_page,jdbcType=INTEGER,mode=IN},#{p_count,jdbcType=INTEGER,mode=OUT},#{p_cursor,mode=OUT,jdbcType=CURSOR,javaType=ResultSet,resultMap=cashClosedPay_map})}
    </select>
    <resultMap id="cashClosedPay_map" type="com.yidu.businessData.pojo.CashClosePayPojo">
        <result property="cashClosedPayId" column="cashClosedPayId" javaType="String"></result>
        <result property="fundId" column="fundId" javaType="String"></result>
        <result property="accountId" column="accountId" javaType="String"></result>
        <result property="securitiesId" column="securitiesId" javaType="String"></result>
        <result property="serviceType" column="serviceType" javaType="Integer"></result>
        <result property="amount" column="amount" javaType="Double"></result>
        <result property="dateTime" column="dateTime" javaType="String"></result>
        <result property="flag" column="flag" javaType="Integer"></result>
    </resultMap>
</mapper>